# signgraph
SignGraph is a graph-based model merging method that clusters parameters by cross-task sign patterns and propagates consensus with personalized PageRank diffusion. It enables stronger multi-task model merging without retraining, outperforming existing baselines across 144 evaluations.
